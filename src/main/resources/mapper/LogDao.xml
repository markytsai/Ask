<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为LogDao接口方法提供sql语句配置 -->
<mapper namespace="com.ilsxh.dao.LogDao">

    <insert id="insertLog">
        insert into log(username, create_date, module_name, operation, work_time, oper_result, user_ip)
        values(#{log.username}, #{log.createDate}, #{log.moduleName}, #{log.operation}, #{log.workTime}, #{log.operResult}, #{log.userIp})
    </insert>


</mapper>

